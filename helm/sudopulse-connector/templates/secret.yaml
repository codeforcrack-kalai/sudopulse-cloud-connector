apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sudopulse-connector.fullname" . }}
  labels:
    {{- include "sudopulse-connector.labels" . | nindent 4 }}
type: Opaque
data:
  token: {{ required "A valid .Values.token is required!" .Values.token | b64enc | quote }}
